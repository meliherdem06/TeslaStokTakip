services:
  - type: web
    name: tesla-stok-takip
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 1 app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.16 